import 'package:flutter/material.dart';

class AppPaddings {
  static const small = EdgeInsets.all(11);
  static const wSmall = EdgeInsets.symmetric(horizontal: 11);
  static const hSmall = EdgeInsets.symmetric(vertical: 11);

  static const semiSmall = EdgeInsets.all(17);
  static const wSemiSmall = EdgeInsets.symmetric(horizontal: 17);
  static const hSemiSmall = EdgeInsets.symmetric(vertical: 17);

  static const extraSmall = EdgeInsets.all(5);
  static const wExtraSmall = EdgeInsets.symmetric(horizontal: 5);
  static const hExtraSmall = EdgeInsets.symmetric(vertical: 5);

  static const lightSmall = EdgeInsets.all(8);
  static const wLightSmall = EdgeInsets.symmetric(horizontal: 8);
  static const hLightSmall = EdgeInsets.symmetric(vertical: 8);

  static const normal = EdgeInsets.all(20);
  static const wNormal = EdgeInsets.symmetric(horizontal: 20);
  static const hNormal = EdgeInsets.symmetric(vertical: 20);

  static const big = EdgeInsets.all(30);
  static const wBig = EdgeInsets.symmetric(horizontal: 30);
  static const hBig = EdgeInsets.symmetric(vertical: 30);

  static const semiBig = EdgeInsets.all(25);
  static const wSemiBig = EdgeInsets.symmetric(horizontal: 25);
  static const hSemiBig = EdgeInsets.symmetric(vertical: 25);

  static const extraBig = EdgeInsets.all(35);
  static const wExtraBig = EdgeInsets.symmetric(horizontal: 35);
  static const hExtraBig = EdgeInsets.symmetric(vertical: 35);
}
